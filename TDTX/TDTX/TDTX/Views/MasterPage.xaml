<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TDTX.Views.MasterPage"
             xmlns:viewModels="clr-namespace:TDTX.ViewModels;assembly=TDTX"
             Title="Tiêu đề"
             BindingContext="{x:Static viewModels:MasterPageViewModel.Instance}">
  <StackLayout VerticalOptions="FillAndExpand" >
    <!--<ListView x:Name="PrimaryListView" VerticalOptions="StartAndExpand" SeparatorVisibility="None" BackgroundColor="Red" 
              ItemsSource="{Binding items}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ImageCell Text="{Binding Title}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>-->
    <ListView x:Name="SubListView" VerticalOptions="EndAndExpand" SeparatorVisibility="None" BackgroundColor="Blue" 
              ItemsSource="{Binding items}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ImageCell Text="{Binding Title}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>